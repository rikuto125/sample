# プロトタイプ検証メモ（2026-06-14）

Playwright で実ブラウザ通しプレイを実施。プロトタイプMVP（4ステージ）の動作を確認した。

## 検証した動線（すべてPASS）

1. **オンボーディング**: 「説明より先に1枚置く」体験 → 成功フィードバック「🎉 いいね！」→ ゲーム開始。
2. **ホーム（ステージマップ）**: 4ステージ表示、未到達はロック（🔒）、進捗バー・累計★表示。
3. **ステージ1（タイムライン・正常系）**: 手札がシャッフル表示 → タップで番号付きスロットに配置 → 正しい時系列で確定 → **STAGE CLEAR ★★★**（ノーミス3星＝採点ロジック正常）。
4. **用語獲得**: クリア時に「ドメインイベント / Domain Event」を獲得、実務リアリティ解説（もっと見る）表示。
5. **ステージ2（ダミー混入）**: コマンド「配達する」（青）・リードモデル「注文状況画面」（緑）が手札に混入し、種別ラベルで色分け表示。ダミーをタップしても**スロットに置かれず弾かれる**（distractor判定が正常）。

## 三重冗長表示（色覚多様性対応・受け入れ条件）

全カードが **色＋アイコン＋ラベル** で種別表現されることを目視確認：
⚡イベント / ▶コマンド / 👤アクター / ⚙ポリシー / ☁外部システム / 📋リードモデル。
aria-label も `${種別}: ${ラベル}` 形式で付与済み。

## 技術指標

- ビルド: gzip 72KB（JS）+ 3.5KB（CSS）。Webフォント読み込み0。
- ユニットテスト: 26件 PASS（判定エンジン13 + ステージデータ整合性13）。
- コンソールエラー: favicon 404 のみ（favicon.svg 追加で解消）。

## 既知の残課題（次イテレーション）

- 効果音・配置スナップアニメ（juice）は最小限。デザイン仕様の音設計は未実装。
- アナリティクスは window キューのみ（GoatCounter等の実バックエンド未接続）。
- ステージ3（例外・外部システム）/ ステージ4（トリガー接続＋因果連鎖）はエンジン・データ・テスト済みだが、実ブラウザ通しプレイは1・2のみ実施（同一エンジンのため動作は担保）。
- D&D並べ替え（dnd-kit sortable）はタップ配置の補助。実機タッチ並べ替えの体感は要確認。
